spring:
  datasource:
    url: jdbc:h2:mem:petclinic-performance;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  sql:
    init:
      data-locations: classpath:data.sql,file:${PERFORMANCE_DATA_FILE:performance/data/data-volumineux.sql}
